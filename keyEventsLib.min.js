function key_down(e){var y=e.keyCode||e.which;keyboard.key[y]=!0}function key_up(e){var y=e.keyCode||e.which;keyboard.key[y]=!1}window.addEventListener("keydown",key_down,!1),window.addEventListener("keyup",key_up,!1);class Keyboard{constructor(){this.key=new Array}getKey(e){return this.key[e]}}keyboard=new Keyboard;

/*
function key_down(e) {
    var key = e.keyCode || e.which;
    keyboard.key[key] = true;
}

function key_up(e) {
    var key = e.keyCode || e.which;
    keyboard.key[key] = false;
}

window.addEventListener("keydown", key_down, false);
window.addEventListener("keyup", key_up, false);

class Keyboard {
    constructor() {
        this.key = new Array();
    }
    
    getKey(e) {
        return this.key[e];
    }
}
keyboard = new Keyboard();
*/

/*
Author: @Favo https://twitter.com/Favo_Gaming
Created at: 13.02.2020 09:27 
*/
